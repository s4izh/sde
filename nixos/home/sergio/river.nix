{
  config,
  pkgs,
  lib,
  sde,
  ...
}:
{
  # wayland.windowManager.river = {
  #   enable = true;
  #   package = null;
  #   systemd.enable = true;
  #   extraConfig = builtins.readFile "${sde.flakeRoot}/dotfiles/.config/river/init";
  # };
}
